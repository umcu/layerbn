Shared helper code used across projects.

- `globals.R` and `helpers.R`: legacy R utilities used in earlier analyses.
- `io.py` and `plotting.py`: placeholders for lightweight Python helpers shared between notebooks.
