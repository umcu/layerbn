# Project-specific settings for METAVCI_COGNITION.
#
# Copy or adapt the keys from `config/global.yml` and `projects/HBC/config.yml`
# to match the METAVCI_COGNITION dataset (paths, SCORE2 region, etc.).
