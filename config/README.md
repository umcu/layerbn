# Configuration

- `global.yml`: defaults and Bayesian-network settings shared across projects.
- `data_paths.example.yml`: template for local data paths (copy to `config/data_paths.yml` and adjust).
- `global.dcf`: legacy configuration kept for reference.
